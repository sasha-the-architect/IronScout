/**
 * Codex CLI agent entrypoint for the documentation drift watcher.
 *
 * Run via:
 *   pnpm exec tsx agents/doc-watcher/index.ts
 *   codex run doc-watcher   (after registering in your Codex config)
 */
import './doc-watcher'
